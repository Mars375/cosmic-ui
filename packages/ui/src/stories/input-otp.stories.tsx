import type { Meta, StoryObj } from '@storybook/react';
import { InputOTP } from '../components/input-otp';

const meta: Meta<typeof InputOTP> = {
  title: 'BaseInputs/InputOTP',
  component: InputOTP,
  tags: ['autodocs'],
};
export default meta;

export const Default: StoryObj<typeof InputOTP> = {};

